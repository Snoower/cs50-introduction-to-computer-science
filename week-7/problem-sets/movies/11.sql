SELECT title FROM movies WHERE id IN (SELECT movies.id FROM movies JOIN stars ON stars.movie_id=movies.id JOIN people ON stars.person_id=people.id JOIN ratings ON ratings.movie_id=movies.id WHERE people.name = 'Chadwick Boseman') ORDER BY (SELECT rating FROM ratings WHERE ratings.movie_id = movies.id) desc LIMIT 5;
